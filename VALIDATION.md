# 验证记录

2026-09-12，Windows，Python 3.13，插件 0.1.0。

| 检查 | 结果 |
|---|---|
| 后端测试（当时 10 项） | 通过：本轮边界、累计差值、去重、重置、半行、迟到统计、路径限制、Windows 扩展路径、未知值和只读资源 |
| 实际 stdio 服务 | 握手、4 个工具、HTML 资源读取通过 |
| 本机真实任务 | 数值与窗口字段读取通过，等待期间 ping 正常，关闭时等待释放 |
| 面板桥接 | 真实无头 Edge + 模拟 MCP 宿主：开始、结束、空等待、暂停恢复、切换任务、旧响应隔离与销毁通过 |
| 布局 | 760、390、280 像素视口无横向溢出；截图为模拟数据 |
| 插件清单与技能 | 校验通过 |
| 个人市场安装 | 已通过桌面 CLI 0.154.0-alpha.6.2 安装，plugin list 确认 installed=true、enabled=true |

安装标识：codex-turn-meter@personal。
本次安装缓存版本：0.1.0+codex.20260912133509。

仍需在新任务中确认：Codex 原生 MCP App 呈现、宿主对 20 秒工具等待的支持、
后续消息产生时旧面板是否仍挂载。模拟宿主测试不等于原生界面验收。
后台通过最长 20 秒的长轮询与每 0.5 秒日志增量检查发现边界；
可见数字按本轮边界更新，非逐 token 更新。没有使用模型刷新循环。

## 对话名称显示更新

2026-09-12，安装缓存版本 0.1.0+codex.20260912135108。
任务选择与观察标签使用本地 threads.name，与客户端命名一致；重名补充项目、更新时间，完全相同时补充序号。任务 ID 继续作为内部绑定键。
已通过真实对话名称读取、10 项后端测试和 Edge 桥接测试（含重名选项与内部绑定）。已重新安装。

## 项目与最后回复排序

2026-09-13，缓存版本 0.1.0+codex.20260913024053。按项目名称分组、组内回复完成时间降序，缺少回复记录置于组末。列表包含全部本地会话。11 项后端测试、Edge 交互测试通过；真实本地 252 个会话中 65 个具有可用回复完成时间。已更新安装。

## 活跃会话过滤与项目解析

2026-09-14。原生实测发现下拉列表被 191 个 `thread_source` 为 subagent / guardian_review
的机器存根会话（无名称、无回复）刷满，且 `project_id` 未关联导致两个同名工作目录
拆成「· 项目 2」重复组。修复后：列表只含用户会话、近 30 天内活跃、上限 50 条，
末尾显示「已省略 N 个较早会话」；分组与组内排序按活跃度（最后回复与最近更新中
较新者）降序，显示日期仍只用最后回复时间；项目名按 `projects` / `project_roots`
最长根路径前缀解析，与客户端侧边栏一致；无名称用户会话用首条用户消息前 30 字
回退命名。旧库缺少 `thread_source` 列时不过滤，保持向后兼容。
14 项后端测试、stdio 冒烟（4 工具、干净关闭）、Edge 模拟宿主交互测试（含已省略
提示项）通过；真实本机 259 条线程中保留 24 条、省略 40 条早期用户会话，存根全部
排除，重复项目组消失。已通过 install.py 更新个人市场安装。原生宿主呈现与卡片
生命周期仍待实机确认。

## 本轮起点未知时回退显示最近请求

2026-09-14，缓存版本 0.1.0+codex.20260914070318。原生实测发现长日志（尾部 8 MB
之外才有本轮 `task_started`）在「本轮提问」范围下主数字全为未知，难以阅读。
修复后：本轮基线未知且最近请求可用时，主数字自动回退为最近请求，范围标签显示
「最近请求 · 本轮起点未知」；统计范围切换与全部口径保持不变，不虚构差值。
14 项后端测试、Edge 模拟宿主交互测试（新增回退断言：数字取最近请求、标签可见、
缓存比例 80.0%）通过；已重新安装并重打包 ZIP（SHA256SUMS 已更新）。

## 仓库改造为第三方 Git 市场

2026-09-14。仓库改为市场布局：插件移至 `plugins/codex-turn-meter/`，
新增 `.agents/plugins/marketplace.json`（市场名 `eliotok`），同步更新
install.py / configure.py / 三个测试脚本的路径引用。验证：14 项后端测试、
Edge 模拟宿主测试通过；validate_plugin 通过；`codex plugin marketplace add`
本地实测能识别 `codex-turn-meter@eliotok`（验证后移除测试源）；个人市场重装
正常。ZIP 内部路径保持 `codex-turn-meter/` 前缀不变，SHA256 与上一版一致。
订阅安装：`codex plugin marketplace add EliotOK/codex-turn-meter` +
`codex plugin add codex-turn-meter@eliotok`。
